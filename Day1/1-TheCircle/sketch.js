// explain setup draw
// explain create canvas
// explain circle
// exlpain coordinate system

function setup() {
    createCanvas(500, 500);
}

function draw() {
    ellipse(mouseX, mouseY, 10, 10)
}